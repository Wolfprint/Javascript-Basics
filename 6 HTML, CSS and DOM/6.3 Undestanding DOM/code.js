'use strict';

//Access All the DOM
/*
    windows is a global object that contains all of the window
*/
console.log(window);
/*
    The DOM is store inside a variable called document in the window object
*/
console.log(window.document);